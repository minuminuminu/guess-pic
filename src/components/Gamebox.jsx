import styled from "styled-components";

const GameboxContainer = styled.div`
  width: 1000px;
  height: 600px;
  border: solid;
  margin: 25px;
`;

export const Gamebox = (props) => {
  return <GameboxContainer></GameboxContainer>;
};
